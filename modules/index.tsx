import { combineReducers } from "redux";
import loading from "./loading";

// redux module 뭉탱이
const rootReducer = combineReducers({loading});
/* redux를 사용할 예정이면 여기에 넣어 사용하면 됩니다  */

export default rootReducer;